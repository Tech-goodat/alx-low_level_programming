#include "main.h"

/**
 *  *_abs - computes absolute value
 *   *
 *    *@i: variable of integer type
 *     *
 *      * Return: 0 always
 *       */

int _abs(int i)
{

		if (i >= 0)
				{
							return (i);
								}
			else
					{
								return (i * -1);
									}
			return (0);
}
